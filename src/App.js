import React from 'react';
import CMap from './CMap'; // Ensure this is the correct path to your Map component

function App() {
  return (
      <div className="App">
          {/* Render the Map component */}
          <CMap /> 
      </div>
  );
}

export default App; // Make sure to export the App component
